# 设计规范 · Aperture Design System

> 适用产品：**证件照工作室**（安卓 Flutter + Windows 桌面 PyQt6 双端共用）
> 设计目标：以**实用性为核心**，用精致的视觉、克制的留白、统一的组件语言，
> 把「本地离线、隐私安全、一键成片」的产品价值做得让人信任且印象深刻。

---

## 1. 设计理念

- **克制而高级**：以中性冷灰为底，单一强调色（Iris 鸢尾靛）点睛，不堆砌颜色。
- **隐私即信任**：「本地离线 · 数据不出本机」不是脚注，而是贯穿全局的信任徽章。
- **工作室质感**：像专业摄影棚——干净的台面、明确的焦点、柔和的投影。
- **一致即专业**：所有页面、所有组件遵循同一套 token，跨平台视觉统一。

设计原则优先级：**功能可用 > 信息清晰 > 操作高效 > 视觉精致**。

---

## 2. 配色方案

### 2.1 强调色（Brand Accent）

| Token | 值 | 用途 |
|---|---|---|
| `accent` | `#5B5BE8` | 主操作、激活态、链接 |
| `accentStrong` | `#4636D6` | 按下/悬停加深 |
| `accentSoft` | `#ECECFB` | 强调色浅底（选中背景、图标按钮底色） |
| `gradient` | `135° #6366F1 → #8B5CF6` | 主按钮填充、Logo、关键引导 |

### 2.2 亮色主题（默认）

| Token | 值 | 用途 |
|---|---|---|
| `canvas` | `#F5F6F8` | 页面背景 |
| `surface` | `#FFFFFF` | 卡片 / 面板 |
| `surface2` | `#FBFBFD` | 浅凸起 / 内嵌区 |
| `surface3` | `#F1F2F6` | 色块按钮 / 芯片底 |
| `ink` | `#14161C` | 主文本 |
| `ink2` | `#5A606E` | 次文本 |
| `ink3` | `#9298A6` | 辅助 / 占位 |
| `border` | `#E7E9EF` | 分割线 / 描边 |
| `borderStrong` | `#D8DBE3` | 强描边 |
| `onAccent` | `#FFFFFF` | 强调色上的文字 |

语义色：`success #1F9D6B` · `warning #E0A106` · `danger #E5484D`

证件照标准底色（色板）：`白 #FFFFFF` · `蓝 #2F6FE0` · `红 #E0494D` · `透明`（棋盘格）

### 2.3 暗色主题

| Token | 值 |
|---|---|
| `canvas` | `#0E0F13` |
| `surface` | `#17191F` |
| `surface2` | `#1C1F27` |
| `surface3` | `#252934` |
| `ink` | `#ECEEF3` |
| `ink2` | `#A4AAB6` |
| `ink3` | `#6E7480` |
| `border` | `#272B34` |
| `borderStrong` | `#363B46` |

强调色与语义色在暗色下保持不变，保证品牌一致。

---

## 3. 字体规范

字体族（按可用顺序回退）：
`Inter` → `PingFang SC` / `Microsoft YaHei` / `Noto Sans SC` → 系统默认。

字阶（基于 4pt 行高栅格）：

| 名称 | 字号 / 行高 | 字重 | 用途 |
|---|---|---|---|
| Display XL | 32 / 40 | 700 | 首页英雄标题 |
| Display L | 26 / 34 | 700 | 大区块标题 |
| Title L | 20 / 28 | 600 | 页面标题 |
| Title M | 17 / 24 | 600 | 区块标题 / 卡片标题 |
| Body L | 16 / 24 | 400/500 | 正文 |
| Body M | 14 / 20 | 400 | 次要正文 |
| Label | 13 / 18 | 600 | 按钮文字 |
| Caption | 12 / 16 | 400 | 说明 / 状态 |
| Micro | 11 / 14 | 500 | 角标 / 倍率 |

字重仅用 400 / 500 / 600 / 700，避免过细字重在小屏发虚。

---

## 4. 间距规则（4pt 栅格）

基础单位 `u = 4px`。命名间距：

| Token | 值 | 典型用途 |
|---|---|---|
| `s1` | 4 | 图标与文字间距 |
| `s2` | 8 | 紧凑内间距 |
| `s3` | 12 | 控件内边距 |
| `s4` | 16 | 卡片内边距 / 元素间距 |
| `s5` | 20 | 区块内边距 |
| `s6` | 24 | 页面边距 / 大区块间距 |
| `s8` | 32 | 区块之间 |
| `s10` | 40 | 章节间距 |
| `s12` | 48 | 大留白 |
| `s16` | 64 | 首屏留白 |

页面默认边距 `s6 (24)`；卡片内边距 `s5 (20)`；列表项间距 `s3 (12)`。

---

## 5. 圆角与阴影

### 半径

| Token | 值 | 用途 |
|---|---|---|
| `rSm` | 8 | 小芯片 / 输入 |
| `rMd` | 12 | 按钮 / 图标按钮 |
| `rLg` | 16 | 卡片 |
| `rXl` | 22 | 底部抽屉 / 大面板 |
| `rPill` | 999 | 胶囊 / 分段控件 |

### 阴影（柔和分层）

| Token | 值 |
|---|---|
| `shadowSm` | `0 1px 2px rgba(20,22,28,.04), 0 1px 3px rgba(20,22,28,.06)` |
| `shadowMd` | `0 6px 16px rgba(20,22,28,.08)` |
| `shadowLg` | `0 16px 40px rgba(20,22,28,.12)` |
| `shadowAccent` | `0 10px 28px rgba(91,91,232,.30)` |

暗色下阴影减弱，改用 1px 描边（`border`）区分层级。

---

## 6. 组件样式

### 6.1 按钮
- **主按钮（Primary）**：渐变填充 `gradient`，`rMd(12)` 圆角，高度 52（大）/ 44（中），Label 13/600，`onAccent` 文字；悬停提亮、按下缩放 `.97` + `shadowAccent`。
- **次按钮（Secondary）**：`surface` 底 + 1px `border`，`ink` 文字；悬停 `surface3`。
- **图标按钮（Tonal）**：`surface3` 圆角 `rMd`，图标 `ink2`；激活态用 `accentSoft` 底 + `accent` 图标。

### 6.2 卡片 / 面板
白底 `rLg(16)` + `shadowSm` + 1px 极淡描边；内边距 `s5`。悬浮卡片（如首页操作卡）用 `shadowMd`，按下 `shadowSm`。

### 6.3 分段控件（Segmented）
胶囊容器 `surface3` 底，激活段 `surface` + `shadowSm` + `accent` 文字；切换有 200ms 滑动指示。

### 6.4 底色色板（Background Swatches）
4 个圆形样本：白 / 蓝 / 红 / 透明（透明用灰白棋盘格表示）。选中态加 2px `accent` 环 + 中心对勾。样本直径 36，环外留 3px。

### 6.5 尺寸预设（Size Preset）
水平分段：一寸 / 二寸 / 标准(500×670) / 大一寸。选中态同分段控件。

### 6.6 开关 / 勾选
iOS 风格开关，开启填充 `accent` 渐变；勾选框选中填充 `accent`。

### 6.7 快照（Snackbar / Toast）
底部浮层，`ink` 深底 `rMd`，白字，3s 自动消失，带轻微上滑入场。

### 6.8 加载状态
AI 推理时画布叠加柔和 shimmer（强调色微光扫过）；主按钮内显示 16px spinner；禁止重复提交。

### 6.9 隐私徽章（Privacy Badge）
`accentSoft` 胶囊 + 锁图标 + 「本地离线 · 数据不出本机」，置于首页顶部与编辑页。

### 6.10 应用标识（App Logo）
`gradient` 渐变圆角方块（rLg），中央白色人像/证件剪影；用于首页英雄区与窗口标题。

---

## 7. 微交互

- **页面转场**：淡入 + 8px 上移（缓动 `ease-out`，时长 220ms）。
- **按钮**：按下 `scale(.97)` 120ms；主按钮释放有涟漪。
- **色板 / 分段**：选中态 200ms 弹性过渡（环 / 滑块）。
- **画布把手**：拖拽时放大 1.15 并增亮，松手回弹。
- **保存成功**：对勾弹入 + 轻震动（移动端）。
- **悬停（桌面）**：控件底色过渡 120ms；主按钮提亮。
- **空状态 / 加载**：骨架屏 + shimmer，避免生硬 spinner。

---

## 8. 图标与插画

- 线性图标为主，stroke 1.8，圆角端点；移动端 24px，桌面 18–20px。
- 优先使用平台图标库（Material Icons / Qt 内置），保持一笔触风格统一。
- 避免拟物插画；用几何形状 + 渐变表达「工作室」调性。

---

## 9. 双端映射

| 设计 Token | Flutter | PyQt6 (QSS) |
|---|---|---|
| 强调渐变 | `AppGradients.primary` | `qlineargradient(135deg, #6366F1, #8B5CF6)` |
| 卡片 | `Container` + `BoxShadow` | `QWidget` + `border-radius:16px; background:#FFF; border:1px solid #E7E9EF` |
| 主按钮 | `AppButton.primary` | `QPushButton` + 渐变 + `border-radius:12px` |
| 底色色板 | `BgSwatch` 组件 | 4 个 `QPushButton` 色块 |
| 间距 | `AppSpacing` | 布局 `setSpacing/setContentsMargins` |
| 字体 | `TextTheme` | `font-family` / `font-size` / `font-weight` |

两端共用同一套色值、字阶、圆角、阴影与微交互时长，确保视觉一致。

---

## 10. 可访问性

- 文本对比度：正文 ≥ 4.5:1，大字 ≥ 3:1。
- 触控目标 ≥ 44×44px（移动端主操作 ≥ 52px）。
- 不应仅以颜色传达状态，辅以文字 / 图标。
- 支持系统字体缩放与暗色模式。
